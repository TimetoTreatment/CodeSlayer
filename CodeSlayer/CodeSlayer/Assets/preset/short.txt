static const unsigned int
static unsigned double
long long int
const char*
const double
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
scanf("%s", str);
printf("%.2lf",PI);
printf("%d", max);
scanf("%d", &n);
atoi("12");
time(NULL);
main(void);
fprintf(fp,"%d", n);
fscanf(fp,"%d", &n);
feof(fp)
getchar();
srand(time(NULL));
rand()%5;
gets(str);
fopen(fp,"data.txt","r");
fclose(fp);
free(ar);
malloc(n*sizeof(int));
calloc(n,sizeof(int));
realloc(ar,m*sizeof(int));